#ifndef BIB_HPP
#define BIB_HPP

#include <vector>

// calcula fatorial (n >= 0). retorna 1 para n = 0.
long fatorial(long n);

// calcula n-ésimo fibonacci (0-based). fibonacci(0)=0, fibonacci(1)=1.
long fibonacci(long n);

#endif // BIB_HPP
